package core

import (
	"testing"
)

func TestLoadScripts(t *testing.T) {
	// logger.SetFlags(loggerLdate | loggerLtime | loggerLshortfile)
	// var opt = libs.Options{}
	// input := "hackerone.com"
	// runner := InitRunner(input, opt)
	// runner.LoadScripts()
}
